
var countingGame = {};

(function(){
  var counter = 0;

  countingGame.startGame1 = function(containerId){
    console.log("starting game1 in container " + containerId);
  };
})();

function sum(a, b, c){
  return Number(a) + Number(b) + Number(c);
};